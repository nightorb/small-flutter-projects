class Character {
  final String name;
  final String image;

  // Ein Constructor, der dafür sorgt, dass die Variablen initialisiert
  // UND benannt werden müssen
  Character({required this.name, required this.image});
}

